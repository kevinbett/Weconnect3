language: python
python:
  - "3.6"
# install dependencies
install:
  - "pip install -r requirements.txt"
  - "pip install coveralls"
# run tests
script:
  - nosetests --with-coverage
branches:
  only:
  - feature
after_success:
  - coveralls
